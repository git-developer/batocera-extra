#!/bin/sh
set -u
echo "$(date -Iseconds)" "${0}" "${@}" >&2

main() {
  service_path="${1}"
  action="${2-}"

  case "${action}" in
    start) "${service_path}/install-and-start" ;;
    stop)  "${service_path}/stop" ;;
    *)
  esac
}

main "${@}"
