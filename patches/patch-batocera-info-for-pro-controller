#!/bin/sh
set -eu

sed -i.backup -E \
  -e 's:/(\{BAT,bat\}\*):/*_\1:g' \
  -e 's/POWER_SUPPLY_CAPACITY=/POWER_SUPPLY_CAPACITY_LEVEL=/g' \
  -e 's/\$\{BATT}%/${BATT}/g' \
  -e 's/(\s*)BATT=.+/\0\n\1if [ "${BATT##*[!0-9]*}" ]; then BATT="${BATT}%"; fi\n/g' \
  "$(command -v batocera-info)"
